// Short landscape on a touch device (phone held sideways): the header AppBar
// turns into a vertical sidebar. Shared so components inside it can adapt too.
export const SIDEBAR_MQ = '@media (orientation: landscape) and (pointer: coarse) and (min-aspect-ratio: 17/10)'
